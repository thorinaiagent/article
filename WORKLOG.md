# Журнал работы

Как выполнялись задачи по этому репозиторию: что просили, по каким шагам шёл,
чем закончилось. Пополняется в конце каждой задачи. Новые записи — сверху.

Формат записи:

```
## ГГГГ-ММ-ДД — короткое название задачи

**Задача:** что просил автор.

**Шаги:**
1. …
2. …

**Результат:** что получилось, куда сохранено.

**Проблемы:** ссылки на пункты в BACKLOG.md (если возникли).
```

---

## 2026-08-12 — Наполнить Google Doc текстом про переезд

**Задача:** добавить в документ на Google Drive текст из файла на компе «Переезд
в коворкинг — рассылка и пост».

**Шаги:**
1. Нашёл файл: `~/Desktop/Переезд/Переезд в коворкинг — рассылка и пост.txt`,
   прочитал (рассылка + пост в телегу).
2. Проверил Google Drive MCP: доступно только создание файлов, нет операций
   изменить/дописать/удалить существующий документ (см. BACKLOG).
3. Раз дозаписать в пустую заготовку нельзя — создал новый Google Doc сразу с
   полным содержимым файла.
4. Прочитал документ обратно, убедился, что весь текст на месте.

**Результат:** новый Google Doc с рассылкой и постом, владелец
i.reshetnikov@redmadrobot.com. Ссылка:
https://docs.google.com/document/d/12y_f3wlEZneJ3-Dt2ohMbFxaP9_RijJud4Se2bYvGmE/edit
Старый пустой документ-заготовку через коннектор удалить нельзя — остаётся на
пользователе.

**Проблемы:** см. BACKLOG — коннектор Google Drive только создаёт файлы.

## 2026-08-12 — Пуш в гит + документ на Google Drive

**Задача:** запушить накопленные изменения и параллельно завести на Google Drive
документ-заготовку «Переезд в коворкинг — рассылка и пост».

**Шаги:**
1. Разделил изменения на два коммита: доработка сборки скилла (транслитерация
   имён файлов) и новое правило «журнал + бэклог».
2. Запушил `main` в `origin` (`c9ea5ce..07cf8bc`), заодно ушёл локальный коммит
   `3d91db8`.
3. Параллельно фоновый агент создал через MCP Google Drive родной Google Doc с
   одной строкой текста «Переезд в коворкинг — рассылка и пост».

**Результат:** изменения на GitHub; документ создан, владелец
i.reshetnikov@redmadrobot.com. Ссылка:
https://docs.google.com/document/d/1rp0pBOkiL7JN3L4s1k-1naEi5gQSfi2hiBtJUt_lp1o/edit

**Проблемы:** нет.

## 2026-08-12 — Правило «журнал работы + бэклог»

**Задача:** завести правило — в конце каждой задачи собирать описание процесса
по шагам и откладывать все возникшие проблемы в бэклог.

**Шаги:**
1. Осмотрел репозиторий, нашёл существующие описания процессов (роутер
   `CLAUDE.md`, скиллы, `README.md`).
2. Уточнил у автора: область (только этот репо), хранение (два файла в корне),
   механизм (правило + хук-напоминание).
3. Создал `WORKLOG.md` и `BACKLOG.md` с шапками-шаблонами.
4. Добавил правило в блок «Всегда, в любой задаче» файла `CLAUDE.md`.
5. Настроил Stop-хук в `.claude/settings.json`: мягкое напоминание через
   `systemMessage`, без блокировки остановки.

**Результат:** правило действует всегда; два файла в корне под гитом; хук
проверен (валидный JSON, команда отрабатывает).

**Проблемы:** см. BACKLOG — активация хука в текущей сессии.
